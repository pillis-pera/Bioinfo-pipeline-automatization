Copy your samples into:
- "merged" for files that contain all the reads of one sample and are ready to be processed.
- "unmerged" for files that have two or more files per sample. Note that sample naming should follow [sampleID][laneID].[filetype]
